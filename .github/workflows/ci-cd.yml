name: CI-CD

on:
  pull_request:
    branches: ["main"]
    types: [opened, synchronize, reopened, closed]
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  test-build:
    runs-on: ubuntu-latest
    env:
      IMAGE_TAG: ${{ github.sha }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 8
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "8"
          cache: maven

      - name: Backend unit tests (JUnit)
        working-directory: backend
        run: mvn -B test

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "18"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Frontend unit tests (JUnit)
        run: bash ci/frontend-unit-test.sh

      - name: Integration tests (docker compose)
        run: bash ci/integration-test.sh

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-reports
          path: |
            backend/target/surefire-reports
            ci/reports

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to DockerHub
        if: github.event_name == 'workflow_dispatch' || github.event_name == 'push'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build & (optionally) push backend image
        if: github.event_name == 'workflow_dispatch' || github.event_name == 'push'
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          push: ${{ github.event_name == 'workflow_dispatch' || github.event_name == 'push' }}
          tags: |
            maonamao/backend:${{ env.IMAGE_TAG }}
            maonamao/backend:latest

      - name: Build & (optionally) push frontend image
        if: github.event_name == 'workflow_dispatch' || github.event_name == 'push'
        uses: docker/build-push-action@v6
        with:
          context: ./frontend
          push: ${{ github.event_name == 'workflow_dispatch' || github.event_name == 'push' }}
          tags: |
            maonamao/frontend:${{ env.IMAGE_TAG }}
            maonamao/frontend:latest

  deploy:
    needs: test-build
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch' || github.event_name == 'push'
    env:
      IMAGE_TAG: ${{ github.sha }}
    concurrency:
      group: ecommerce-deploy
      cancel-in-progress: true
    steps:
      - name: Prepare SSH private key
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
        run: |
          set -e
          # ========== 关键修改1：严格配置.ssh目录+文件权限链 ==========
          # 1. 清空旧目录（避免残留权限问题）
          rm -rf .ssh
          # 2. 创建.ssh目录，权限必须是700（仅当前用户访问）
          mkdir -p .ssh
          chmod 700 .ssh
          # 3. 写入私钥文件，权限必须是600（仅当前用户读写）
          python - <<'PY'
          import os
          key = os.environ.get('SSH_PRIVATE_KEY', '')
          if not key.strip():
              raise SystemExit('SSH_PRIVATE_KEY is empty')
          key = key.replace('\r\n', '\n')
          with open('.ssh/github_actions', 'w', encoding='utf-8') as f:
              f.write(key)
          PY
          chmod 600 .ssh/github_actions
          # 4. 验证权限（日志中可查看，方便排错）
          ls -la .ssh/
          ls -la .ssh/github_actions
          ssh-keygen -lf .ssh/github_actions || echo "私钥格式错误，请检查Secrets"

      - name: Deploy via SSH (k3s kubectl)
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          # ========== 关键修改2：移除无效的key_permissions参数 ==========
          # ========== 关键修改3：保留key_path，确保路径正确 ==========
          key_path: .ssh/github_actions
          port: ${{ secrets.SSH_PORT }}
          fingerprint: "SHA256:AG7nRpWC+NVXcUCumX0/e+A9Qucyb3yLsIFycJif6Tg"
          debug: true
          # ========== 关键修改4：删除无用的passphrase/password（避免干扰） ==========
          # passphrase: ${{ secrets.SSH_PASSPHRASE }}
          # password: ${{ secrets.SSH_PASSWORD }}
          script: |
            set -e
            cd ${{ secrets.DEPLOY_PATH }}
            git pull

            if command -v k3s >/dev/null 2>&1; then
              KUBECTL="k3s kubectl"
            else
              KUBECTL="kubectl"
            fi

            $KUBECTL apply -f k8s/ecommerce/00-namespace.yml
            $KUBECTL apply -f k8s/monitoring/00-namespace.yml

            $KUBECTL apply -f k8s/ecommerce
            $KUBECTL apply -f k8s/monitoring

            cat <<'EOF' | $KUBECTL apply -f -
            apiVersion: v1
            kind: Secret
            metadata:
              name: mysql-secret
              namespace: ecommerce
            type: Opaque
            stringData:
              MYSQL_ROOT_PASSWORD: ${{ secrets.MYSQL_ROOT_PASSWORD }}
              MYSQL_DATABASE: ${{ secrets.MYSQL_DATABASE }}
              MYSQL_USER: ${{ secrets.MYSQL_USER }}
              MYSQL_PASSWORD: ${{ secrets.MYSQL_PASSWORD }}
            EOF

            cat <<'EOF' | $KUBECTL apply -f -
            apiVersion: v1
            kind: Secret
            metadata:
              name: grafana-admin
              namespace: monitoring
            type: Opaque
            stringData:
              admin-user: ${{ secrets.GRAFANA_ADMIN_USER }}
              admin-password: ${{ secrets.GRAFANA_ADMIN_PASSWORD }}
            EOF

            TAG="${{ env.IMAGE_TAG }}"

            $KUBECTL -n ecommerce set image deployment/frontend frontend=maonamao/frontend:$TAG

            ACTIVE_TRACK=$($KUBECTL -n ecommerce get svc backend -o jsonpath='{.spec.selector.track}')
            if [ "$ACTIVE_TRACK" = "blue" ]; then
              INACTIVE_TRACK="green"
            else
              INACTIVE_TRACK="blue"
            fi

            $KUBECTL -n ecommerce set image deployment/backend-$INACTIVE_TRACK backend=maonamao/backend:$TAG
            $KUBECTL -n ecommerce rollout status deployment/backend-$INACTIVE_TRACK --timeout=120s

            $KUBECTL -n ecommerce patch svc backend -p '{"spec":{"selector":{"app":"backend","track":"'"$INACTIVE_TRACK"'"}}}'

            $KUBECTL -n ecommerce rollout status deployment/frontend --timeout=120s